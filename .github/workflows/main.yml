name: 'Flyway Info'
 
on:
  push:
    branches:
      - master
 
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Run Flyway Migrate
        uses: docker://redgate/flyway:10
        with:
          args: >-
            info
            -workingDirectory="/github/workspace/"
            -url="jdbc:sqlserver://localhost;authentication=sqlPassword;databaseName=SQLDocker_QA;encrypt=true;trustServerCertificate=true"
            -user="Redgate"
            -password="Redg@te1"
